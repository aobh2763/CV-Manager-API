services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: cvmanager
      MYSQL_ROOT_PASSWORD: secret
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  adminer:
    image: adminer 
    ports:
      - "8080:8080"

volumes:
  mysql_data:
